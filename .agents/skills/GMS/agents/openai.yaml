interface:
  display_name: "GMS"
  short_description: "Repo-specific patterns and workflows for GMS"
  default_prompt: "Use the GMS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true